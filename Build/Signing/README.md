# Signing Directory

Place your Android keystore file here. This directory is gitignored.

See `PUBLISHING.md` for keystore generation instructions.

**Files that go here:**
- `leaderboard-game.keystore` — Android signing keystore

⚠️ NEVER commit keystore files to git.
